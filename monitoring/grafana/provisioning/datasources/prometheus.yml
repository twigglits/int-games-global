apiVersion: 1

# Provisioned at start-up, so the dashboard works with no manual setup.
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    uid: movie-search-prometheus
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 10s
      httpMethod: POST
    editable: false
